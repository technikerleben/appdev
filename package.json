{
  "name": "appdev-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Android EPUB reader and Küchenblatt TRMNL web app",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "npx vercel dev",
    "test": "node --test tests/*.test.js"
  }
}
